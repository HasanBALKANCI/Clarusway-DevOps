apiVersion: v1
kind: Service
metadata:
  name: {{ include "hasan-chart.fullname" . }}
  labels:
    {{- include "hasan-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hasan-chart.selectorLabels" . | nindent 4 }}
